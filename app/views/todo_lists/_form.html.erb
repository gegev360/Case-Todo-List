<div id="new_item_form">
  <%= form_with(model: [todo_list, todo_list.items.build], data: { turbo_stream: true }) do |form| %>
    <div class="mb-3">
      <%= form.label :description, "Descrição", class: "form-label" %>
      <%= form.text_area :description, class: "form-control", rows: 3, required: true %>
    </div>
    <%= form.submit "Adicionar Item", class: "btn btn-primary w-100" %>
  <% end %>
</div> 