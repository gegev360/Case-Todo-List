<h2>Esqueci minha senha</h2>

<p>Digite seu e-mail para receber instruções de redefinição de senha.</p>

<%= form_with url: password_resets_path, method: :post do |form| %>
  <div class="field">
    <%= form.label :email %>
    <%= form.email_field :email, required: true, autofocus: true %>
  </div>

  <div class="actions">
    <%= form.submit "Enviar instruções" %>
  </div>
<% end %>

<div class="links">
  <%= link_to "Voltar para login", login_path %>
</div> 